/*!
 *
 * Holder - 2.2 - client side image placeholders
 * (c) 2012-2013 Ivan Malopinsky / http://imsky.co
 *
 * Provided under the MIT License.
 * Commercial use requires attribution.
 *
 * */
var Holder = Holder || {};
! function(Q, P) {
  function O(e, d) {
    var c = "complete",
      b = "readystatechange",
      a = !1,
      p = a,
      o = !0,
      n = e.document,
      m = n.documentElement,
      l = n.addEventListener ? "addEventListener" : "attachEvent",
      i = n.addEventListener ? "removeEventListener" : "detachEvent",
      g = n.addEventListener ? "" : "on",
      k = function(q) {
        (q.type != b || n.readyState == c) && (("load" == q.type ? e : n)[i](g + q.type, k, a), !p && (p = !0) && d.call(e, null))
      },
      h = function() {
        try {
          m.doScroll("left")
        } catch (q) {
          return void setTimeout(h, 50)
        }
        k("poll")
      };
    if (n.readyState == c) {
      d.call(e, "lazy")
    } else {
      if (n.createEventObject && m.doScroll) {
        try {
          o = !e.frameElement
        } catch (f) {}
        o && h()
      }
      n[l](g + "DOMContentLoaded", k, a), n[l](g + b, k, a), e[l](g + "load", k, a)
    }
  }

  function N(a) {
    a = a.match(/^(\W)?(.*)/);
    var c = document["getElement" + (a[1] ? "#" == a[1] ? "ById" : "sByClassName" : "sByTagName")](a[2]),
      b = [];
    return null !== c && (b = c.length ? c : 0 === c.length ? c : [c]), b
  }

  function M(d, c) {
    var b = {};
    for (var a in d) {
      d.hasOwnProperty(a) && (b[a] = d[a])
    }
    for (var a in c) {
      c.hasOwnProperty(a) && (b[a] = c[a])
    }
    return b
  }

  function L(e, b, g) {
    b = parseInt(b, 10), e = parseInt(e, 10);
    var d = Math.max(b, e),
      c = Math.min(b, e),
      a = 1 / 12,
      f = Math.min(0.75 * c, 0.75 * d * a);
    return {
      height: Math.round(Math.max(g.size, f))
    }
  }

  function J(i) {
    var e = i.ctx,
      d = i.dimensions,
      c = i.template,
      b = i.ratio,
      a = i.holder,
      p = "literal" == a.textmode,
      o = "exact" == a.textmode,
      m = L(d.width, d.height, c),
      l = m.height,
      h = d.width * b,
      g = d.height * b,
      k = c.font ? c.font : "sans-serif";
    z.width = h, z.height = g, e.textAlign = "center", e.textBaseline = "middle", e.fillStyle = c.background, e.fillRect(0, 0, h, g), e.fillStyle = c.foreground, e.font = "bold " + l + "px " + k;
    var f = c.text ? c.text : Math.floor(d.width) + "x" + Math.floor(d.height);
    if (p) {
      var d = a.dimensions;
      f = d.width + "x" + d.height
    } else {
      if (o && a.exact_dimensions) {
        var d = a.exact_dimensions;
        f = Math.floor(d.width) + "x" + Math.floor(d.height)
      }
    }
    var n = e.measureText(f).width;
    return n / h >= 0.75 && (l = Math.floor(0.75 * l * (h / n))), e.font = "bold " + l * b + "px " + k, e.fillText(f, h / 2, g / 2, h), z.toDataURL("image/png")
  }

  function H(f, h, b, e) {
    var d = b.dimensions,
      c = b.theme,
      a = b.text ? decodeURIComponent(b.text) : b.text,
      g = d.width + "x" + d.height;
    c = a ? M(c, {
      text: a
    }) : c, c = b.font ? M(c, {
      font: b.font
    }) : c, h.setAttribute("data-src", e), b.theme = c, h.holder_data = b, "image" == f ? (h.setAttribute("alt", a ? a : c.text ? c.text + " [" + g + "]" : g), (A || !b.auto) && (h.style.width = d.width + "px", h.style.height = d.height + "px"), A ? h.style.backgroundColor = c.background : (h.setAttribute("src", J({
      ctx: K,
      dimensions: d,
      template: c,
      ratio: I,
      holder: b
    })), b.textmode && "exact" == b.textmode && (w.push(h), D(h)))) : "background" == f ? A || (h.style.backgroundImage = "url(" + J({
      ctx: K,
      dimensions: d,
      template: c,
      ratio: I,
      holder: b
    }) + ")", h.style.backgroundSize = d.width + "px " + d.height + "px") : "fluid" == f && (h.setAttribute("alt", a ? a : c.text ? c.text + " [" + g + "]" : g), h.style.height = "%" == d.height.slice(-1) ? d.height : d.height + "px", h.style.width = "%" == d.width.slice(-1) ? d.width : d.width + "px", ("inline" == h.style.display || "" === h.style.display || "none" == h.style.display) && (h.style.display = "block"), A ? h.style.backgroundColor = c.background : (w.push(h), D(h)))
  }

  function F(a, c) {
    var b = {
      height: a.clientHeight,
      width: a.clientWidth
    };
    if (!b.height && !b.width) {
      if (a.hasAttribute("data-holder-invisible")) {
        throw new Error("Holder: placeholder is not visible")
      }
      return a.setAttribute("data-holder-invisible", !0), setTimeout(function() {
        c.call(this, a)
      }, 1), null
    }
    return a.removeAttribute("data-holder-invisible"), b
  }

  function D(b) {
    var f;
    f = null == b.nodeType ? w : [b];
    for (var c in f) {
      if (f.hasOwnProperty(c)) {
        var a = f[c];
        if (a.holder_data) {
          var e = a.holder_data,
            d = F(a, D);
          d && (e.fluid && a.setAttribute("src", J({
            ctx: K,
            dimensions: d,
            template: e.theme,
            ratio: I,
            holder: e
          })), e.textmode && "exact" == e.textmode && (e.exact_dimensions = d, a.setAttribute("src", J({
            ctx: K,
            dimensions: e.dimensions,
            template: e.theme,
            ratio: I,
            holder: e
          }))))
        }
      }
    }
  }

  function C(c, b) {
    var e = {
        theme: M(G.themes.gray, {})
      },
      d = !1;
    for (sl = c.length, j = 0; sl > j; j++) {
      var a = c[j];
      Q.flags.dimensions.match(a) ? (d = !0, e.dimensions = Q.flags.dimensions.output(a)) : Q.flags.fluid.match(a) ? (d = !0, e.dimensions = Q.flags.fluid.output(a), e.fluid = !0) : Q.flags.textmode.match(a) ? e.textmode = Q.flags.textmode.output(a) : Q.flags.colors.match(a) ? e.theme = Q.flags.colors.output(a) : b.themes[a] ? b.themes.hasOwnProperty(a) && (e.theme = M(b.themes[a], {})) : Q.flags.font.match(a) ? e.font = Q.flags.font.output(a) : Q.flags.auto.match(a) ? e.auto = !0 : Q.flags.text.match(a) && (e.text = Q.flags.text.output(a))
    }
    return d ? e : !1
  }
  var B = !1,
    A = !1,
    z = document.createElement("canvas"),
    y = 1,
    x = 1,
    w = [];
  if (z.getContext) {
    if (z.toDataURL("image/png").indexOf("data:image/png") < 0) {
      A = !0
    } else {
      var K = z.getContext("2d")
    }
  } else {
    A = !0
  }
  A || (y = window.devicePixelRatio || 1, x = K.webkitBackingStorePixelRatio || K.mozBackingStorePixelRatio || K.msBackingStorePixelRatio || K.oBackingStorePixelRatio || K.backingStorePixelRatio || 1);
  var I = y / x,
    G = {
      domain: "holder.js",
      images: "img",
      bgnodes: ".holderjs",
      themes: {
        gray: {
          background: "#eee",
          foreground: "#aaa",
          size: 12
        },
        social: {
          background: "#3a5a97",
          foreground: "#fff",
          size: 12
        },
        industrial: {
          background: "#434A52",
          foreground: "#C2F200",
          size: 12
        },
        sky: {
          background: "#0D8FDB",
          foreground: "#fff",
          size: 12
        },
        vine: {
          background: "#39DBAC",
          foreground: "#1E292C",
          size: 12
        },
        lava: {
          background: "#F8591A",
          foreground: "#1C2846",
          size: 12
        }
      },
      stylesheet: ""
    };
  Q.flags = {
    dimensions: {
      regex: /^(\d+)x(\d+)$/,
      output: function(b) {
        var a = this.regex.exec(b);
        return {
          width: +a[1],
          height: +a[2]
        }
      }
    },
    fluid: {
      regex: /^([0-9%]+)x([0-9%]+)$/,
      output: function(b) {
        var a = this.regex.exec(b);
        return {
          width: a[1],
          height: a[2]
        }
      }
    },
    colors: {
      regex: /#([0-9a-f]{3,})\:#([0-9a-f]{3,})/i,
      output: function(b) {
        var a = this.regex.exec(b);
        return {
          size: G.themes.gray.size,
          foreground: "#" + a[2],
          background: "#" + a[1]
        }
      }
    },
    text: {
      regex: /text\:(.*)/,
      output: function(a) {
        return this.regex.exec(a)[1]
      }
    },
    font: {
      regex: /font\:(.*)/,
      output: function(a) {
        return this.regex.exec(a)[1]
      }
    },
    auto: {
      regex: /^auto$/
    },
    textmode: {
      regex: /textmode\:(.*)/,
      output: function(a) {
        return this.regex.exec(a)[1]
      }
    }
  }, document.getElementsByClassName || (document.getElementsByClassName = function(b) {
    var f, c, a, e = document,
      d = [];
    if (e.querySelectorAll) {
      return e.querySelectorAll("." + b)
    }
    if (e.evaluate) {
      for (c = ".//*[contains(concat(' ', @class, ' '), ' " + b + " ')]", f = e.evaluate(c, e, null, 0, null); a = f.iterateNext();) {
        d.push(a)
      }
    } else {
      for (f = e.getElementsByTagName("*"), c = new RegExp("(^|\\s)" + b + "(\\s|$)"), a = 0; a < f.length; a++) {
        c.test(f[a].className) && d.push(f[a])
      }
    }
    return d
  }), window.getComputedStyle || (window.getComputedStyle = function(a) {
    return this.el = a, this.getPropertyValue = function(b) {
      var c = /(\-([a-z]){1})/g;
      return "float" == b && (b = "styleFloat"), c.test(b) && (b = b.replace(c, function() {
        return arguments[2].toUpperCase()
      })), a.currentStyle[b] ? a.currentStyle[b] : null
    }, this
  }), Object.prototype.hasOwnProperty || (Object.prototype.hasOwnProperty = function(b) {
    var a = this.__proto__ || this.constructor.prototype;
    return b in this && (!(b in a) || a[b] !== this[b])
  });
  for (var E in Q.flags) {
    Q.flags.hasOwnProperty(E) && (Q.flags[E].match = function(a) {
      return a.match(this.regex)
    })
  }
  Q.add_theme = function(b, a) {
    return null != b && null != a && (G.themes[b] = a), Q
  }, Q.add_image = function(f, c) {
    var b = N(c);
    if (b.length) {
      for (var d = 0, a = b.length; a > d; d++) {
        var e = document.createElement("img");
        e.setAttribute("data-src", f), b[d].appendChild(e)
      }
    }
    return Q
  }, Q.run = function(d) {
    B = !0;
    var b = M(G, d),
      q = [],
      p = [],
      o = [];
    for ("string" == typeof b.images ? p = N(b.images) : window.NodeList && b.images instanceof window.NodeList ? p = b.images : window.Node && b.images instanceof window.Node && (p = [b.images]), "string" == typeof b.bgnodes ? o = N(b.bgnodes) : window.NodeList && b.elements instanceof window.NodeList ? o = b.bgnodes : window.Node && b.bgnodes instanceof window.Node && (o = [b.bgnodes]), n = 0, a = p.length; a > n; n++) {
      q.push(p[n])
    }
    var f = document.getElementById("holderjs-style");
    f || (f = document.createElement("style"), f.setAttribute("id", "holderjs-style"), f.type = "text/css", document.getElementsByTagName("head")[0].appendChild(f)), b.nocss || (f.styleSheet ? f.styleSheet.cssText += b.stylesheet : f.appendChild(document.createTextNode(b.stylesheet)));
    for (var c = new RegExp(b.domain + '/(.*?)"?\\)'), a = o.length, n = 0; a > n; n++) {
      var m = window.getComputedStyle(o[n], null).getPropertyValue("background-image"),
        k = m.match(c),
        h = o[n].getAttribute("data-background-src");
      if (k) {
        var l = C(k[1].split("/"), b);
        l && H("background", o[n], l, m)
      } else {
        if (null != h) {
          var l = C(h.substr(h.lastIndexOf(b.domain) + b.domain.length + 1).split("/"), b);
          l && H("background", o[n], l, m)
        }
      }
    }
    for (a = q.length, n = 0; a > n; n++) {
      var i, g;
      g = i = m = null;
      try {
        g = q[n].getAttribute("src"), attr_datasrc = q[n].getAttribute("data-src")
      } catch (e) {}
      if (null == attr_datasrc && g && g.indexOf(b.domain) >= 0 ? m = g : attr_datasrc && attr_datasrc.indexOf(b.domain) >= 0 && (m = attr_datasrc), m) {
        var l = C(m.substr(m.lastIndexOf(b.domain) + b.domain.length + 1).split("/"), b);
        l && (l.fluid ? H("fluid", q[n], l, m) : H("image", q[n], l, m))
      }
    }
    return Q
  }, O(P, function() {
    window.addEventListener ? (window.addEventListener("resize", D, !1), window.addEventListener("orientationchange", D, !1)) : window.attachEvent("onresize", D), B || Q.run()
  }), "function" == typeof define && define.amd && define([], function() {
    return Q
  })
}(Holder, window),
/*!
 * JavaScript for Bootstrap's docs (http://getbootstrap.com)
 * Copyright 2011-2014 Twitter, Inc.
 * Licensed under the Creative Commons Attribution 3.0 Unported License. For
 * details, see http://creativecommons.org/licenses/by/3.0/.
 */
! function(b) {
  b(function() {
    var e = function(k) {
      var h = 1;

      function c(m) {
        var o = m.attr("id");
        if (!o) {
          o = "_j_id_" + h++;
          m.attr("id", o);
          console.log(o)
        }
        var n = m.data("title") || m.html();
        var l = $('<li><a href="#' + o + '">' + n + "</a></li>");
        return l
      }
      var i = $(".bs-docs-container h1");
      var d = i.length - 1;
      i.each(function(m, o) {
        o = $(o);
        var l = c(o);
        var n;
        if (m != d) {
          n = o.nextUntil("h1", "h2");
          if (!n.length) {
            n = o.nextUntil("h1", "h3")
          }
        } else {
          n = o.nextAll("h2");
          if (!n.length) {
            n = o.nextAll("h3")
          }
        }
        if (n.length > 0) {
          var p = $("<ul>").addClass("nav");
          n.each(function(q, r) {
            p.append(c($(r)))
          });
          l.append(p)
        }
        k.append(l);
        l.hide();
        l.fadeIn()
      })
    };
    if (navigator.userAgent.match(/IEMobile\/10\.0/)) {
      var a = document.createElement("style");
      a.appendChild(document.createTextNode("@-ms-viewport{width:auto!important}")), document.querySelector("head").appendChild(a)
    }
    var g = b(window),
      f = b(document.body);
    b(".navbar").outerHeight(!0) + 10;
    f.scrollspy({
      target: ".bs-sidebar"
    }), g.on("load", function() {
      f.scrollspy("refresh")
    }), b(".bs-docs-container [href=#]").click(function(c) {
      c.preventDefault()
    }), setTimeout(function() {
      var c = b(".bs-sidebar");
      e(c.find("ul"));
      b(document.body).scrollspy("refresh");
      c.affix({
        offset: {
          top: function() {
            var i = c.offset().top,
              h = parseInt(c.children(0).css("margin-top"), 10),
              d = b(".bs-docs-nav").height();
            return this.top = i - d - h
          },
          bottom: function() {
            return this.bottom = b(".bs-footer").outerHeight(!0)
          }
        }
      })
    }, 100), setTimeout(function() {
      b(".bs-top").affix()
    }, 100), b(".toolt ip-demo").tooltip({
      selector: "[data-toggle=tooltip]",
      container: "body"
    }), b(".tooltip-test").tooltip(), b(".popover-test").popover(), b(".bs-docs-navbar").tooltip({
      selector: "a[data-toggle=tooltip]",
      container: ".bs-docs-navbar .nav"
    }), b("[data-toggle=popover]").popover(), b("#loading-example-btn").click(function() {
      var c = b(this);
      c.button("loading"), setTimeout(function() {
        c.button("reset")
      }, 3000)
    })
  });
  $(".btn").addClass("btn-css");
  $("img").addClass("img-responsive img-md");
  $("div.carousel-inner div img, #download-row div .download-icon, table tbody tr td img, #vpon-logo, .jumbo-list-item div img").removeClass("img-md");
  $("#vpon-logo, .img-example").removeClass("img-responsive");
  $("#btn-to-download").click(function() {
    $("html, body").animate({
      scrollTop: $("#download").offset().top
    }, 1000)
  })
}(jQuery);
